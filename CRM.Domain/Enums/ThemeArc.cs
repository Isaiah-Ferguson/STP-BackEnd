namespace CRM.Domain.Enums;

/// <summary>
/// The production arc a month belongs to, from the Year Calendar legend:
/// Green = Foundational Reset, Gold = Spring Show, Rose = Nutcracker.
/// </summary>
public enum ThemeArc
{
    FoundationalReset,
    SpringShow,
    Nutcracker,
}
